# Environment files with secrets — never commit these
.env
.env.local
.env.*.local
.env.production

# Ignore local rate limiter file-based fallback
api/.rate_limits.json
storage/rate_limits/

# Ignore PHPStorm / IDE files
.idea/
*.iml

# Ignore Composer vendor directory if created
vendor/
